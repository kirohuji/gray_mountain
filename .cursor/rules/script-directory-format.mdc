---
description: 剧本与开发文档的目录结构、文件命名、格式规范 — plays（纯文学层）与 scripts（游戏开发层）
globs: docs/plays/**,docs/scripts/**
alwaysApply: true
---

# 剧本目录结构与格式规范

## 一、两个层级的职责

| 层级 | 路径 | 受众 | 内容 |
|------|------|------|------|
| **plays**（纯文学层） | `docs/plays/` | 编剧、叙事设计 | 纯叙事文本、对话、场景描述。不含任何游戏元数据。 |
| **scripts**（游戏开发层） | `docs/scripts/` | 程序、美术、音效 | 同一场景 + 元数据头（地点/时间/角色/bg/bgm/sfx）+ 可交互物品 + 旗标/触发逻辑。 |

**核心规则：两层目录结构必须完全镜像。** 同名章节 → 同名场景文件，一一对应。

## 二、目录结构

```
docs/plays/
  chXX-标题/                 # 章节目录（编号沿用 docs/stories/ 的章节号）
    _章节总览.md             # 章节级：场景总览表 + 叙事结构 + 核心意象（纯文学视角）
    01-场景名.md             # 场景文件：纯叙事（每个场景一个 .md）
    02-场景名.md
    ...

docs/scripts/
  chXX-标题/                 # ← 目录名与 plays 完全一致
    _章节总览.md             # 章节级：场景总览表 + 旗标表 + 探索数据（游戏开发视角）
    01-场景名.md             # 场景文件：开发文档（元数据头 + 叙事 + 交互逻辑）
    02-场景名.md
    ...
  (全局参考文档可放在 scripts/ 根，如 游戏设定与叙事风格.md)
```

**示例：**

```
docs/plays/ch11-归途/01-玄关.md
docs/scripts/ch11-归途/01-玄关.md
```

## 三、命名规范

### 章节目录

```
ch{两位数字}-{中文标题}
```

- 编号**必须**是两位数字：`ch11`、`ch02`（不是 `ch2`）
- 编号**沿用** `docs/stories/` 中的章节编号，便于跨层对照
- 标题与 stories 对应章节标题一致

✅ `ch11-归途`  `ch12-异变`  `ch03-抉择`
❌ `play01-归途`  `第一章-归途`  `ch1`

### 场景文件

```
{两位数字}-{场景名}.md
```

- 编号是场景在该章节内的序号：`01`、`02`……`08`
- 场景名简洁描述地点或事件：`玄关`、`厨房`、`她`
- 最终收束场景用数字编号（不用 `F`）

✅ `01-玄关.md`  `04-走廊-卧室门.md`  `08-她.md`
❌ `scene-01-玄关.md`  `Scene F：她.md`  `1.md`

## 四、plays 场景文件格式（纯文学层）

只写叙事文本。**禁止出现**任何游戏元数据（bg/bgm/sfx/旗标/触发条件）。

```markdown
# 01：玄关

（场景切。画面渐亮。）

> 哈里斯大道的公寓和你离开时一模一样。窄长的棕色石砖公寓……

吉姆帮你开了门，把行李靠墙放在鞋柜旁。

> 「药在茶几上——哈丁医生开的，每晚一片。」
>
> 「我明天再来看你。」

（门关上的声音。钥匙转动的声音。）

> 然后——完全地、彻底地——安静了。

---

## 可交互物品

### 行李袋
> 深灰色的帆布袋靠在鞋柜侧面……
```

格式要点：
- 标题 `# 01：场景名`
- 舞台指示用 `（中文括号）`
- 对话用 `> 「」` 引用块
- 内心低语用 `> *(斜体括号)*`
- 物品交互用 `### 物品名` 三级标题
- 大段落分隔用 `---`

## 五、scripts 场景文件格式（游戏开发层）

**必须**以元数据头（front matter）开头，然后是叙事文本和交互逻辑。

```markdown
# 01：玄关

- 地点：哈里斯大道公寓 / 玄关
- 时间：傍晚
- 角色：汤姆、吉姆
- 背景：bg_apartment_entry_evening
- BGM：none
- SFX：door_open, key_turn, door_close

---

## 叙事文本

（此处为完整的纯叙事内容，结构与 plays 层一致）

---

## 可交互物品

### 1. 行李袋

- 类型：信息物品
- 位置：鞋柜侧面
- 旗标：`examined_bag`

> 深灰色的帆布袋靠在鞋柜侧面……

### 2. 电话答录机

- 类型：信息物品
- 位置：玄关柜
- 旗标：`heard_voicemail`

> 玄关柜上的电话答录机。红灯正在闪烁……

---

## 触发与分支

- 进入条件：章节开始时自动加载
- 离开条件：至少交互 2 个物品 / 点击走廊方向
- 旗标设置：`entered_entryway` = true
```

### 元数据头字段

| 字段 | 说明 | 示例 |
|------|------|------|
| 地点 | 大地图 / 子区域 | `哈里斯大道公寓 / 玄关` |
| 时间 | 一天中的时段 | `傍晚`、`深夜`、`清晨` |
| 角色 | 本场景出场角色，顿号分隔 | `汤姆、吉姆` |
| 背景 | Ren'Py 背景图像标识 | `bg_apartment_entry_evening` |
| BGM | 背景音乐，无则 `none` | `none`、`bgm_piano_low_01` |
| SFX | 音效，逗号分隔，无则 `none` | `door_open, key_turn, door_close` |

### 可交互物品字段

```markdown
### N. 物品名

- 类型：信息物品 / 关键物品 / 象征物品
- 位置：所在子区域
- 旗标：`flag_name`（可选）
- 条件：`flags.get("xxx")` 或 `anomaly_count >= 3`（可选）

> 描述文本……
```

## 六、_章节总览.md 格式（两层各自维护）

每个章节目录下放置一个 `_章节总览.md`。**plays 层和 scripts 层的章节总览侧重点不同**——plays 层聚焦叙事结构与文学母题；scripts 层聚焦游戏逻辑数据。同名章节的总览文件应保持编号和场景名一致，但内容按各自受众撰写。

### plays 层章节总览（纯文学视角）

面向编剧和叙事设计。只写叙事结构、核心意象、人物弧线、伏笔衔接。**禁止出现**游戏元数据（旗标、触发条件、`anomaly_count` 等）。

```markdown
# ch01 归途 · 章节总览

## 场景总览

| 编号 | 场景 | 叙事功能 | 氛围基调 |
|------|------|---------|---------|
| 01 | 玄关 | 开场与氛围建立 | 寂静、陌生感、归属感的丧失 |
| 02 | 厨房 | 第一波不安的细节积累 | 日常物的异化、记忆的不可靠 |
| ... | ... | ... | ... |

## 出场人物

| 角色 | 出场场景 | 备注 |
|------|---------|------|
| 汤姆（主角） | 全部 | 第一人称视角，全程在场 |
| ... | ... | ... |

## 叙事结构

- 空间推进：从外部向内推进
- 情感弧线：归来 → 观察 → 不安 → 发现 → ……
- 节奏控制：渐强的细节积累 → 裂缝扩大 → 情感爆发与悬置

## 核心意象与母题

### 灰白色粉末
- 贯穿全章的视觉母题
- 出现的场景及叙事功能
- 暗示的主题方向

### 其他意象
- ……

## 衔接与伏笔

| 伏笔 | 出现场景 | 指向 |
|------|---------|------|
| ... | ... | ... |
```

### scripts 层章节总览（游戏开发视角）

面向程序、美术、音效。汇总该章节的游戏逻辑数据：场景信息表、旗标定义、探索数据、全局触发规则。

```markdown
# ch11 归途 · 章节总览

## 场景总览

| 编号 | 场景 | 类型 | 可交互点数 |
|------|------|------|-----------|
| 01 | 玄关 | 探索入口 | 4 |
| 02 | 厨房 | 探索房间 | 3 |
| ... | ... | ... | ... |

## 章节旗标

| 旗标 | 类型 | 条件 | 影响 |
|------|------|------|------|
| `explored_kitchen` | bool | 进入厨房并交互 >=1 次 | 后续厨房对话变化 |
| ... | ... | ... | ... |

## 探索数据

| 区域 | 房间 | 可交互物品数 | 异常物品数 |
|------|------|-------------|-----------|
| ... | ... | ... | ... |

## 全局触发

- 探索 >=4 个交互点 → 触发电话铃响 → 推进到 Scene 08（她）
```

## 七、场景编号的一些说明

- 场景编号在章节内从 `01` 开始递增
- 收束场景（原 Scene F）使用数字编号，不用字母
- 如果后续需要插入场景，可以分配中间编号（如 `03a-xxx.md`），但更推荐从一开始就留好编号空间
- plays 和 scripts 的同名场景文件编号必须一致

## 八、全局文档（scripts 根目录）

`docs/scripts/` 根目录可放置不归属于特定章节的全局参考文档：

- `游戏设定与叙事风格.md` — 世界观、叙事风格、视觉/音频风格、人物档案
- `注意事项.md` — 设计决策记录（如"玩家不知道女主死了"原则）

这些文档**不需要**在 plays 层镜像——它们属于游戏开发层的全局参考资料。
